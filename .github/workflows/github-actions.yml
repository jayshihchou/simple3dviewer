name: replace string in index.html
on: [push]
jobs:
  replace_file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls ../ -R
      - run: sed -i s+src/index.js?n=1+src/index.js?${{ github.run_number }}+ simple3dviewer/index.html
