name: replace string in index.html
on: [deployment]
jobs:
  replace_file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sed -i s+src/index.js?n=1+src/index.js?${{ github.run_number }}+ index.html
